# docker-compose.override.yml
# Automatically merged with docker-compose.yml when running `docker-compose up`.
# Safe to leave empty — add per-developer overrides here (never commit secrets).
#
# Example: enable verbose API logging locally
# services:
#   api:
#     environment:
#       Logging__LogLevel__Default: Debug
